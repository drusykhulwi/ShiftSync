version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: shiftsync-postgres
    environment:
      POSTGRES_DB: shiftsync
      POSTGRES_USER: shiftsync
      POSTGRES_PASSWORD: shiftsync123
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U shiftsync"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
